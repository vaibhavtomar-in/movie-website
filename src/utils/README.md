This folder contains utils in the react app.

utils (short for utilities) are small, reusable JavaScript functions or helper modules designed to perform specific, common tasks across the application. They are generally stateless, meaning they take input, process it, and return a result without managing component state or side effects. 